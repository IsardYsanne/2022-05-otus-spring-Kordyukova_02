spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/library_test
    username: userdb
    password: pass

  sql:
    init:
      mode: always
      #      never
      #      always

  shell:
    interactive:
      enabled: false